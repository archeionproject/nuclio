version: '3.8'

services:
  nuclio-dashboard:
    image: quay.io/nuclio/dashboard:latest-amd64
    container_name: nuclio-dashboard
    ports:
      - "8070:8070"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock